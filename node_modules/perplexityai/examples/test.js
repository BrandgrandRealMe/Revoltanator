const PerplexityAI = require('../src/');

const main = async() => {
  const prompt = 'que problema existe con las isapres en chile?';
  const response = await PerplexityAI.search(prompt);
  console.log('response', response);
}

main();