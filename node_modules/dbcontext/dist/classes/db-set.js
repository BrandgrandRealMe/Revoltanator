import Linq from "linq-utils";
class DbSet extends Linq {
    name;
    constructor(name, items = []) {
        super(items);
        this.name = name;
    }
}
export default DbSet;
//# sourceMappingURL=db-set.js.map